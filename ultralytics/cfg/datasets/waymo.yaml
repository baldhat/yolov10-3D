# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../datasets/waymo

train: train_third_entire_dataset.json
val: val.json
test: test.json
# trainval: ImageSets/trainval.txt

# Classes
names:
  0: Car
  1: Pedestrian #FIXME
  2: Cyclist